import React from "react";
import "../components/Object/SpeObject.scss";
import Nav from "./Nav";

const Header = () => (
    <div className={`header-nasa-meteor`}>
        <div className={`container-fluid text-center pl-0 pr-0 pt-2 pb-2 bg-primary text-white`}>
            MyMeteor
        </div>
    </div>

);

export default Header;
